# Build ledger: docs/hackathon-build/plan.md

Merge base: c87129d (kickoff, spec, plan). Commits stay local until Hao Wen says "push" (github.com/zghanw/openlc is public).

Task 1: complete (commits ee7ca8d..5cff1e3). Review found 3 issues (env example described the future, dotfiles skipped, renamed defaults); fixed in 1 round, re-review clean. Baseline: backend 103 tests pass, backend + web build.
Task 2: complete (commits d46dfe7..b6a7832). Review: no critical issues; fixed in 1 round: restored Move conservation check in _settle, auth-first approveSettlement, cumulative releasedAmount (invariant total == released + balance + settledBuyerRefund), /types/ ignored. Re-review clean.
Task 3: complete (commits d183420..6ea6815). 41 tests pass: 15 Move-parity + validation + approvals + exact deadline boundaries + terminal state + hostile receiver + reentrancy + invariants. Review found 2 critical blind spots (reentrancy test insensitive to the guard; inspection-window rule tested tautologically); both fixed and proven by mutation testing (removing nonReentrant, and breaking max(shippedAt, deliveryDeadline), each fail a named test). Written by the orchestrator after the subagent hit the session limit.
Task 4: complete (commits abf8854..008132c). OpenLCEscrow deployed to BOT Chain testnet at 0x20C3b91B78D6F86b27C01e12692d2e56C0bcA5C5 (block 24026875, tx 0x1349fb2c...), source-verified (explorer API is_verified=true, solc 0.8.24, optimizer 200). ABI exported to backend and web, check-abi green. Review found a mainnet-day trap (a flaky-RPC re-run could double-deploy and silently overwrite a good record) plus a missing network field and a stale-ABI hole; all 7 findings fixed and re-review clean. Deploy now refuses to overwrite a record without ALLOW_REDEPLOY=yes and refuses mainnet without CONFIRM_MAINNET=yes.
Task 5: complete (commits a0b3170..HEAD). MetaMask EIP-191 sign-in replaces Sui/zkLogin/Enoki; the Supabase Google token path is gone too, so a wallet session is the only way in (fails closed: no identity configured = hard 401). Identity tables renamed to openlc_accounts / openlc_wallet_identities. Backend tests 103 -> 105. Review: approved, no critical; it confirmed single-use challenge consumption is atomic in Postgres and checksum comparison is canonical. The implementer caught that the challenge table still required a 64-hex Sui address, which would have failed every real sign-in. Follow-up applied: the signed message now names the configured FRONTEND_ORIGIN instead of a forgeable request header, and wallet routes require a 20-byte address.
NOTE for task 6: the zod validators in backend/src/api/app.ts are still Sui-shaped - transactionDigest is base58 (rejects 0x EVM hashes) and packageId/escrowObjectId accept 1-64 hex. They must become EVM-shaped or recording funding will reject valid transactions.
Task 6: complete (commits d059ee7..ecf00c2). EVM verifiers replace both Sui verifiers: every step re-read from BOT Chain and refused unless the transaction itself proves it. Stronger than the Sui original in one respect: tx.from pins each step to the wallet that signed it. Sui-shaped request validators fixed (base58 hashes would have rejected every real BOT Chain transaction). @mysten/sui fully removed from the backend. Tests 105 -> 121. Review ran a check-by-check parity table against the Sui originals: no critical findings, one real gap fixed (verifyFullRelease trusted a fresh state read instead of the submitted transaction, on a wrong assumption that releaseFull emits no event - the shared _settle always emits SettlementExecuted), plus per-method lookalike-contract and wrong-signer tests that were previously covered on only one method out of eight.
Task 7: complete (commits f0a98da..c3ee760). Invites and funding now bind to wallets, not emails (sign-in is MetaMask-only, so email binding would have made every invite unacceptable). Named supplier wallet: only that wallet accepts. Bearer link: first wallet to accept is bound, no second wallet can take it. Callers can no longer self-declare a payout address - it comes only from the authenticated session. Tests 121 -> 129. Review found a CRITICAL payout-redirection path: on an email-bound order accepted by a walletless session, supplierWalletAddress stayed undefined and all three funding guards were written "if (value && !match) throw", so they skipped silently and the buyer could direct the payout anywhere (demonstrated by an existing test). Fixed: supplier acceptance always binds a wallet, and the funding guards now require presence before comparing (SUPPLIER_WALLET_REQUIRED / ARBITRATOR_WALLET_REQUIRED). createOrder also rejects self-dealing and colliding party wallets.
Task 8 (task 9 merged in): complete (commits c987b4b..94c5376). The browser moved from Sui dApp Kit to MetaMask + ethers: wallet provider with network switch/add, wallet sign-in, and all nine escrow actions on ethers with exact 18-decimal BOT maths (parseUnits, never floats) and seconds on-chain vs milliseconds in the API converted once. Work spanned a session-limit interruption; the agent was resumed rather than restarted. Review found a CRITICAL dead end: after a successful chain step, a failed API call left the order unrecordable because every retry re-signed and the contract reverted - and the same class affected ship, accept, refund and claim, not just the claim. Fixed with one shared withRecording helper: the record is saved after confirmation and before posting, and a retry re-posts it without re-signing; the claim can also recover its transaction from on-chain logs. Also added: withdraw() for bounced payouts, a wallet-drift guard that blocks signing as a different wallet than the session, and a fail-closed arbitrator address (no more 0xccc placeholder). NOT yet verified against real MetaMask - the click-by-click plan for task 11 is in .hackathon-build/plan/task-8-report.md.
Task 10: complete (commits 2083df0..0e2d31d). The web app is OpenLC-named and Sui-free: the DoD grep is empty (it was 224 lines in 37 files). A "Built on BOT Chain" footer mark (botchain.ai, the active network's explorer, and the escrow contract once configured) renders on every route. An interim "LC" mark replaces PayProof's "P" logo and favicon. The wallet page shows the BOT balance with a faucet (testnet) or DEX (mainnet) link, the explorer and copy-address; the simulated card/bank top-up, QR pay and bank withdraw are gone, and task 8's owed withdraw() stays. The unlinked /console page (Google demo sign-in) and the dead Google helpers are deleted. The web env var is now NEXT_PUBLIC_OPENLC_BACKEND_URL (use that name on Vercel in task 11), and the trust-profile field newOnPayProof is now newOnOpenLC in backend and web. Review: the haiku implementer twice reported a clean grep that wasn't (51 lines, then 15). It also broke three page titles ("OpenLC · &" from a sed backreference) and left one CSS class unstyled. It took 2 fix rounds, the second gated by .hackathon-build/plan/task-10-check.sh (13 checks, all PASS). The orchestrator also found that the live-order "Open claim without signing (demo)" button posted a base58 placeholder the EVM validator rejects (a 400 on every click); the button is removed. Verified by the orchestrator: web build, units test, backend 129 tests; the production build shows the footer and titles on /, /workspace, /orders, /wallet and /buyer, and /console returns 404. Parked: docs/terms-of-service.md and docs/dispute-policy.md still say ProofPay/Sui/zkLogin (task 13; every footer links to them). The web/tests/e2e specs target the removed Google/zkLogin flows (task 19). Dead Supabase-OAuth code remains in web/lib/openlc-api.ts (restoreSupabaseSession, no callers). The backend /auth/demo/google route and the PAYPROOF_* backend env names remain (task 15, ideally renamed before Render is configured in task 11).
Task 11 part A (deploy prep): done (commits 14906df..3617982). Task 11 stays open until part B: Hao Wen sets up Render and Vercel, then the Feature Zero MetaMask run-through. The backend is Render-ready: tsx is a runtime dependency, `start` no longer passes --use-system-ca (older Node versions reject it at boot), engines is node >=22, and a boot smoke through the real `npm start` returns {"ok":true,"service":"openlc-api"}. The env names are final before any dashboard is configured: OPENLC_SESSION_SECRET, OPENLC_DEMO_MODE, OPENLC_DOCUMENTS_BUCKET. User-visible PayProof defaults were fixed in the SQL before its first apply (every new wallet would have got a workspace named "My PayProof workspace"), along with the fallback party name, a Sui error message and the health service name. .github/workflows/keepalive.yml pings vars.API_HEALTH_URL every 10 minutes and skips while that variable is unset. All 12 migrations are applied to Supabase ysxjtlajqyunlqspktuv, verified through the MCP: 12 migrations, 8 public tables with RLS, and a private openlc-documents bucket. Security advisors: 2 INFO (service-role-only tables without policies) and 1 WARN (current_payproof_account_id is a security definer function callable by authenticated), all pre-existing by design. Review: 1 critical. The implementer applied an edited identities migration without committing it, and its drop/create policy names didn't match (a re-run would fail). Fixed in 1 round; re-review clean. Accepted as is: tsx ^4.23.15 instead of the pinned 4.23.13. Found for task 13: with GEMINI_API_KEY set, the API also requires QDRANT_URL/QDRANT_API_KEY at boot, so Render runs without Gemini until mediation is configured.
Task 11: complete (commits 14906df..454a3c1). Feature Zero passes on the deployed preview with real MetaMask: API on Render (https://openlc-api.onrender.com), web on Vercel (https://openlc.vercel.app), Supabase ysxjtlajqyunlqspktuv, keep-alive workflow green, ESCROW_VERIFIER_ENABLED=true. Two orders ran end to end on testnet escrow 0x20C3b91B78D6F86b27C01e12692d2e56C0bcA5C5, every step marked verified_on_chain by the API. PO-90758439 (3 BOT, happy path): fund + 0.3 deposit 0x846ea2b8874fa2bfdfa2c36ef42b0801b65504a8127014fe9de49b257da6c46f; ship + 0.6 dispatch, Shipped.evidenceHash = the SHA-256 of the dispatch photo, 0x2098aacdbe5ff33d5d971b906e55ff5798cd3a092ada5a11d1099b072bd29005; full acceptance releasing 2.1 0x793669d83aa0a3e0ca5a78ec8c6c8d0aa495ed40bed455328f0c3584bada57ed. PO-97139111 (1 BOT, claim path): fund + 0.1 deposit 0x8f215a07dbe962e31b5ce86070a17c05a3c4b5d5b22bee6c39c1bb1bcdc0b33b; ship + 0.2 dispatch 0xd8bff88315199b0a36b16c9c7b361ad0eeded6775668aed130ac4d60bb4746af; damage photo anchored 0x2f4631e0741b19cd99004ab3b82b7bbc12a301ecce0c35f88c75923499c220eb; THE HEADLINE, one transaction holding 0.15 disputed and paying the supplier the undisputed 0.55, 0x8a72ab5e9f79100ee522063024443288a8bba20f23c634aa2a78667c6060f97a; both parties approved the same split (0x456a3a26..., 0x0b83f4cd...) and executeSettlement refunded 0.15 in mode MutualApproval, 0xb57dc85208eee87e171db06dbcecc370ad310d382c9af0101ae014d6fe220e61. Five production-only bugs surfaced in the run-through, each fixed test-first and verified against the live API: (1) every signed-in request 401'd because openlc_wallet_identities has UNIQUE(account_id), so PostgREST embeds the wallet as an object and the store read [0]; (2) no invite could be opened or accepted, because previewInvite/acceptInvite still demanded an email match and wallet sessions carry none; (3) the accept returned 400 INVALID_REQUEST because the web sends email:"" and the schema ran .email() on it; (4) funding could never send, because the stored orderHash has no 0x prefix and ethers rejects an unprefixed bytes32; (5) claim amounts carried float noise (3*0.15 = 0.44999999999999996), now rounded to 9 decimals inside toUnits before parseUnits. An intermediate fix that let any email bypass the invite token was caught in review and reverted. Also: Vercel silently missed one push (no deployment, no status), so verify a deploy by its commit status plus a marker string unique to the new build, never a string that already existed. Open for task 13: the settlement execution is not recorded by the API (dispute still settlement_pending / pending_on_chain though SettlementExecuted is on chain); the buyer saw an error whose text is still needed.
Brand: the interim "LC" mark is replaced by Hao Wen's OpenLC mark, a white molecule on a black squircle, cropped to its own edges and shipped as web/public/favicon.png (256x256, transparent corners), used for the favicon, the apple icon and every logo slot; .brand-logo-mark no longer paints a background behind it (commits d2b1b2f, ce86d42).
Task 12: complete (commits d446dc4..5cf76c6). A lone judge can now finish the main action with one wallet: the create-order dialog offers "Use the OpenLC demo supplier" ("Confirms instantly. Does not ship. Use a second wallet to try shipment."), and the order is supplier_confirmed in the same request that mints its invite, so the next click is Fund escrow. The browser only ever sends a boolean: the wallet comes from OPENLC_DEMO_SUPPLIER_ADDRESS in the server's own config, so no caller can bind an arbitrary "demo" wallet, and the server still holds no key for it. The auto-accept reuses the real acceptWithInvite path with a synthetic actor, so every guard still runs (initiator authority, already-accepted, party distinctness, wallet binding) and the returned invite token is already spent. IdentityService gained findOrCreateWalletAccount for the system account. Demo orders default to 0/0/100 with a note that nothing is released until acceptance and the full amount is reclaimable after the delivery date if the demo supplier never ships - that protects a judge's funds on mainnet. Backend tests 134 -> 138. Review: the backend half was approved with no guard bypasses; one CRITICAL frontend bug was caught. Creating a demo order takes two HTTP calls, and if the second never lands the order persists with no supplierEmail, which web/lib/live-orders.ts:95 read without an optional chain, crashing the entire orders list (TradeOrder.supplierEmail was typed as required, hiding it from tsc). Fixed in 1 round; re-review clean. Accepted as is: the auto-accept keys on the address matching the configured demo wallet rather than on the request flag (a caller posting that address directly gains nothing the flag would not give them). NEEDS FROM HAO WEN: set OPENLC_DEMO_SUPPLIER_ADDRESS on Render (0x73e709f27a9f1AB7588F0a619a7B0d09eAeCc8e0, the supplier wallet, which must never be the buyer or the arbitrator); until then the option fails closed with DEMO_SUPPLIER_NOT_CONFIGURED (503).
Task 12 follow-up: complete (commit 446fb40). On the live demo path the order named its supplier "My OpenLC workspace", not "OpenLC Demo Supplier": the auto-accept reuses acceptWithInvite, whose name precedence puts the accepting account's workspace first, and the demo wallet is a real account (the second test wallet) whose workspace was auto-provisioned with that default. It overwrote both supplierName and confirmation.organizationName ("Confirmed by My OpenLC workspace" on the order page). Fixed at the one write: acceptWithInvite takes an optional displayName, passed only by createInvite's demo branch from the server-configured name, so no request field can reach it and a real supplier's workspace name still wins on every other path. The demo wallet's workspace itself is not renamed, since it is a real account. The task-12 tests had missed it because they ran with organizations = undefined; they now run a real OrganizationService, and the first demo test asserts both names on the returned and stored order. Mutation-checked: the old source fails it with exactly the live symptom. Backend 138 tests pass, tsc clean. Review: approved, no critical or important findings; two minors accepted as is (displayName in the buyer branch is unreachable today, kept for symmetry; the demo wallet's own /trust page still shows its real workspace name, and no demo order links there). Verified on the live API after the Render deploy: check-demo-supplier.mjs returns supplier_confirmed, supplier name "OpenLC Demo Supplier". Existing live demo orders keep the old name (not backfilled).
Task 13: code complete and live (commits 49ca271..86456fc); its DoD run is still owed (Hao Wen: a fresh two-wallet claim through AI mediation, both signatures, execution). Root cause of the unrecorded settlement on PO-97139111, reproduced by running the real EvmSettlementVerifier against the live chain: it compared EscrowCreated.amount (the whole 1 BOT order) with dispute.totalEscrowUnits, which trade-service sets to the delivery tranche still held when the claim opens (0.7 BOT). So no order that released a deposit or dispatch payment before a claim could ever record its settlement (inherited verbatim from the Sui original). The fix compares EscrowCreated.delivery; the test fixture is now production-shaped and failed before the fix. Recovered live: Hao Wen pressed Record settlement on PO-97139111 and the dispute is settled / verified_on_chain with 0xb57dc85208eee87e171db06dbcecc370ad310d382c9af0101ae014d6fe220e61 at block 24318390, no new signature. Web: executeSettlement was the one chain-then-record step outside withRecording; it now uses it, and an InvalidState revert (escrow already settled) recovers the SettlementExecuted transaction from chain logs. The claim reads buyer/supplier/arbitrator approvals from the chain, enables Execute only when the contract would accept it, shows Record settlement once the escrow is settled, and does not offer a pointless signature after the arbitrator signed. The API boots with Gemini and no Qdrant (legal-authority retrieval only feeds the human arbitration package). Gemini: gemini-3.1-flash-lite (the old default) returned 503 "high demand" on every call for the whole evening and 2.5 models return 404 for this key, so GEMINI_MODEL is now an ordered fallback list (default gemini-3-flash-preview,gemini-3.5-flash; the last one is retried), and an outage abstains with "The AI mediator is busy right now" instead of the false "failed deterministic safety validation". Even so, every model 503'd for a stretch around 23:00 MYT: mediation depends on Google's capacity at demo time. Terms of Service and Dispute Resolution Policy are OpenLC version 1.2: BOT Chain, wallet sign-in, link invites, the demo supplier, what is sent to Gemini (statements and transcripts, never raw files), pilot liability on testnet and mainnet; all 64 DP clause ids unchanged, mediation prompts name the OpenLC policy. Gate .hackathon-build/plan/task-13-check.sh: 15/15. Backend 139 -> 144 tests. Review: approved, no critical; 1 important (the Sign button ignored an arbitrator approval and a settled escrow), fixed in 1 round with the Gemini fallback; haiku re-review clean. Verified live: the mediate route answers 404 for an unknown dispute (not 503 AI_UNAVAILABLE), so Render has the mediator configured.
Task 14: complete (commits 4db4394..7056d44). The order page has an "On BOT Chain" panel read straight from getEscrow: escrow number, status and settlement mode, amount locked, released to the supplier, still held, disputed, delivery deadline, inspection close, and an explorer link, with its own loading and "BOT Chain is not reachable" states. A warning appears when the OpenLC record disagrees with the chain. Reclaim (buyer) and Claim (supplier) appear only when a pure deadlineAction port of refundUnshipped/claimUninspected says the contract would accept the call, with an assert check (web/lib/deadline-action.check.mjs); an RPC failure falls back to the old API-deadline behavior. Live proof, headless on testnet: a throwaway buyer funded escrow #3 with a 150-second deadline through the live API and the demo supplier; deadlineAction said null just before the deadline and reclaim just after; refundUnshipped returned the full 0.01 BOT 32 seconds after the deadline (chain: status Settled, mode RefundUnshipped, never shipped) and the API recorded the order settled and verified. Review: 1 CRITICAL (the panel and the deadline controls kept the pre-action chain read on screen after a step succeeded, so a judge would see a false "not yet shipped" warning right after shipping; the same stale state leaked across order-to-order navigation, and a failed re-read rendered "not reachable" beside stale numbers), fixed in 1 round; haiku re-review clean. Deployed: Vercel success, marker string confirmed in the live bundle.
