import type { Metadata } from "next";

export const metadata: Metadata = { title: "Dispute console | ProofPay" };

export default function ConsoleLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
