import type { Metadata } from "next";
export const metadata: Metadata = { title: "Trust profile | ProofPay", description: "Manage the verified activity shown on your company trust profile." };
export default function TrustLayout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
